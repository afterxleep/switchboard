# FlowDeck Coding Task

You are a coding agent working on the FlowDeck iOS build toolchain.

## Repository

The repository is at the current working directory. This is a git worktree for this specific issue.

## Task

{{ISSUE_IDENTIFIER}}: {{ISSUE_TITLE}}

{{ISSUE_DESCRIPTION}}

## Requirements

- Write clean, well-tested Swift code
- Follow existing patterns in the codebase
- Run `swift test` to verify your changes pass
- Create a commit with message: "{{ISSUE_IDENTIFIER}}: <description>"
- Push the branch to origin when complete

## Git Setup

Before committing, set git identity:
```
git config user.email "james@flowdeck.studio"
git config user.name "James Harper"
```

## Important Rules

- NEVER auto-assign Linear issues to anyone
- Only modify code relevant to this task
- If something is unclear, make a reasonable assumption and document it
- DO NOT post any GitHub PR comments, issue comments, or review responses — ever
- DO NOT use `gh pr comment`, `gh issue comment`, or any GitHub CLI comment command
- Communication with reviewers is handled by a separate agent — your job is code only
